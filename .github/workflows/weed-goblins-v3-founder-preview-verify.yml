name: Weed Goblins V3 Founder Preview Verify

on:
  push:
    branches:
      - feature/weed-goblins-v3-starter-lands

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Run V3 Starter Lands tests
        run: node --test src/features/games/weed-goblins/v3/*.test.js
      - name: Run existing regression suite with diagnostics
        id: regression
        continue-on-error: true
        shell: bash
        run: |
          set +e
          npm test 2>&1 | tee regression.log
          status=${PIPESTATUS[0]}
          exit $status
      - name: Upload regression diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: weed-goblins-v3-regression-log
          path: regression.log
          retention-days: 1
      - name: Build production bundle
        run: npm run build
      - name: Fail if existing regression suite failed
        if: steps.regression.outcome == 'failure'
        run: exit 1
